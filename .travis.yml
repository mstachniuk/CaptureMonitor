language: shell
os: windows
before_install:
  - choco install python27
  - python -m pip install --upgrade pip
env: PATH=/c/Python27:/c/Python27/Scripts:$PATH

install:
  - pip install --upgrade pip
  - pip install pylint
  - pip install -r requirements.txt

script:
  pylint src/main.py
